/**
 * Опциональная локальная конфигурация для «РКН Симулятор».
 *
 * Чит-меню сравнивает SHA-256 введённого пароля с ожидаемым хэшем.
 * По умолчанию встроенный хэш задан в index.html — текст пароля в репозитории не хранится.
 *
 * Чтобы сменить пароль на свой:
 * 1. Вычислите SHA-256 от UTF-8 строки пароля (64 hex-символа).
 *    Node: require('crypto').createHash('sha256').update('ваш_пароль','utf8').digest('hex')
 * 2. Раскомментируйте строку ниже и подставьте hex.
 */
// window.__RKN_CHEAT_HASH_HEX__ = '';
